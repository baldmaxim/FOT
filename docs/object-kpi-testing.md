# KPI закрытия КС-2 — как протестировать от начала до конца

Инструкция для приёмки модуля «KPI объектов» (Этап 1). Проходится за 15–20 минут: назначаем руководителя строительства объекту, делаем офисного сотрудника экономистом, вводим договор и акты, сверяем расчёт.

Предварительно должны быть применены миграции `241_object_kpi_ks2.sql`, `242_object_kpi_pages_and_economist.sql` и `243_object_ks6_entries.sql` (строго в этом порядке) и задеплоен бэкенд.

## 0. Как устроен экран

Вверху одной строкой: **План за период · Факт КС-2 · Выполнение · Объект · «Назначения»**. Виджеты считают **весь расчёт до текущего месяца** — по всем объектам сразу или по одному, если объект выбран. Использованный период подписан строкой ниже («Период: январь 2025 — август 2026»); глубже 10 лет отчёт не уходит.

Таблица по умолчанию не показывается: она появляется, когда выбран объект, и сразу содержит **все расчётные месяцы договора**, свежий сверху. Тогда же над ней появляются **плановая/фактическая ЗОС, контрольная дата** (по самому свежему месяцу) и кнопка **«Создать договор»**. Крестик внутри поля объекта (виден при наведении) возвращает «Все объекты» и убирает таблицу.

Кнопка «Создать договор» всегда открывает **пустую форму нового договора**. Существующий договор открывается кликом по строке месяца. Если договор у объекта уже есть, сохранение вернёт «У объекта уже есть договор — правьте существующий»: в системе один договор на объект.

---

## 1. Кто что видит

| Роль | Где | Что может |
|---|---|---|
| **Экономист** (`economist`) | «Аналитика» → вкладка «KPI объектов» | Вводит договоры, допсоглашения и акты КС-2, фиксирует план, назначает руководителей. Видит все объекты |
| **Руководитель (строительство)** (`manager_obj`) | «Личный кабинет» → «Мои объекты» | Только смотрит расчёт по своим закреплённым объектам. Вкладки «KPI объектов» у него нет |
| **Руководитель эк. отдела** | там же, где экономист | Дополнительно может пересматривать план уже закрытого месяца. Это **не** системная роль, а отметка поверх текущей — выдаётся в модалке «Назначения» и только администратором |
| **Администратор** | везде | Всё вышеперечисленное |

Права страниц уже прописаны миграцией 242, вручную ничего выдавать не нужно.

---

## 2. Назначить объект руководителю строительства

1. Войдите администратором или экономистом.
2. Меню → **«Аналитика»** → вкладка **«KPI объектов»**.
3. Кнопка **«Назначения»** в правом верхнем углу.
4. В форме:
   - **Роль** — «Руководитель строительства»;
   - **Объект** — выберите из списка (список берётся из справочника объектов СКУД);
   - **Сотрудник** — начните вводить фамилию, от двух букв появится подсказка, выберите человека;
   - **Период с** — дата, с которой он отвечает за объект (для теста поставьте `01.01.2025`), **по** оставьте пустым — это «бессрочно».
5. **«Назначить»**.

**Как проверить:**
- строка появилась в таблице «Действующие закрепления» внизу модалки;
- закройте модалку — в отчёте в колонке «Руководитель» у этого объекта стоит ФИО.

**Ограничения, которые сработают намеренно:**
- второго руководителя на тот же объект с пересекающимся периодом система не пропустит (ошибка «На объекте уже есть руководитель строительства в этот период») — сначала закройте прежнее закрепление кнопкой «Закрыть сегодня»;
- экономистов объекта можно назначать сколько угодно.

### Что увидит сам руководитель

Пусть он войдёт под собой: **«Личный кабинет» → «Мои объекты»**. Вверху — плашка премии за выбранный месяц: сумма, «база × коэффициент», шкала с отметкой выполнения, четыре виджета (План КС-2 · Факт КС-2 · Выполнение · Коэффициент К) и расчёт строками, который можно проверить глазами. Ниже — таблица «Расчёт по месяцам» (клик по строке переключает плашку), итоги за период и карточка на каждый объект с месяцами, долей руководителя и его планом/фактом. Редактировать он ничего не может — это правильно.

**Расчёт предварительный**: коэффициент считается по шкале приказа (80→0,00; 85→0,25; 90→0,50; 95→0,80; 100→1,00; 105→1,25; 110→1,50, между точками линейно), премия — от базы 200 000 ₽ пропорционально дням закрепления. Публикации, возражений и «Утверждено» в системе пока нет.

Месяц не получает премию и объясняет почему: «шкала не действует» (месяцы до 01.08.2026), «не были закреплены», «план не определён» (п. 4.5), «нет данных по объекту» — последнее означает, что у одного из объектов нет договора или плановой ЗОС, и частичную сумму система намеренно не показывает.

Если раздел «Мои объекты» в меню не появился — у человека роль не `manager_obj`. Либо смените роль (раздел 3), либо выдайте его роли доступ к странице «Мои объекты (KPI)» в «Роли и доступы».

---

## 3. Сделать офисного сотрудника экономистом

1. Меню → **«Пользователи»**.
2. Найдите человека поиском, кликните по строке — карточка раскроется.
3. Селект **«Должность:»** → выберите **«Экономист»**. Сохранение происходит сразу, появится «Должность изменена».

**Два важных следствия — предупредите человека заранее:**

- **Роль у пользователя одна.** «Экономист» вытеснит текущую («Офисный сотрудник» и т. п.) со всеми её правами. Если прежнюю роль отбирать нельзя — заводите отдельного пользователя, а не выдавайте право роли «Офисный сотрудник»: её носят сотни людей, и доступ к деньгам объектов получат все.
- **Смену роли сопровождает принудительный выход.** Текущая сессия станет недействительной, человек войдёт заново — это нормально.

**Как проверить:** пусть экономист войдёт. В меню есть «Аналитика», внутри — единственная вкладка «KPI объектов». Вкладки «Дисциплина» у него нет, и это ожидаемо.

---

## 4. Где экономист вводит данные по объекту

Отдельной кнопки «Добавить объект» нет: объекты берутся из справочника СКУД и перечислены в выпадающем списке. Пока по объекту нет договора, вместо таблицы выводится «По объекту нет договора».

Кнопка **«Создать договор»** открывает пустую форму — в этом режиме показана только вкладка «Договор и ЗОС», после сохранения карточка закрывается. Существующий договор открывается **кликом по строке месяца**: там шесть вкладок, а кнопка **«Сохранить»** есть в футере на каждой из них и применяет то, что изменено на текущей вкладке. Строки-черновики в таблицах правятся прямо в ячейках; подписанные — только аннулированием.

### «Договор и ЗОС»
Номер и дата договора, заказчик, **стоимость с НДС** (поле с разделителями тысяч), плановая ЗОС, фактическая ЗОС (заполняется, когда объект сдан), первый расчётный месяц.

- Плановая ЗОС — ключевое поле: от неё считается контрольная дата (**ЗОС + 3 месяца**) и число расчётных месяцев. Без неё месяц уходит в статус «нет плановой ЗОС» и в расчёт не попадает.
- «Первый расчётный месяц» обрезает историю: если договор подписан в середине года, укажите его, иначе в отчёте появятся пустые месяцы с 0 %. Поле выбирается **месяцем**, не конкретной датой — расчёт всегда ведётся от начала месяца.
- Поле «Основание правки» появляется в футере, только когда у объекта уже есть зафиксированные месяцы, — и тогда оно обязательно.

### «Допсоглашения»
№, дата, сумма (можно отрицательную). Отдельного поля «действует с» нет: **дата ДС и есть дата вступления в силу**. Запись создаётся черновиком, в расчёт войдёт только после кнопки **«Подписать»**.

Граница учёта: ДС, действующий **с 1-го числа**, входит в план этого же месяца; ДС от 2-го числа и позже — в план следующего. Уже зафиксированный месяц он не пересчитывает.

### «КС-2»
Вид записи («КС-2» или «Уменьшение объёма»), №, **дата подписания заказчиком**, сумма. Сумма всегда вводится положительной — знак для уменьшения система поставит сама. Запись создаётся черновиком, в факт попадает после **«Подписать»**.

Расчётный месяц определяется именно датой подписания заказчиком, а не датой составления акта.

### «КС-6»
№, дата, сумма — журнал учёта выполненных работ. **Справочный реестр: в план, остаток, факт и процент КС-6 не входит** (приказ считает KPI по подписанным КС-2). В отчёте он показан отдельной колонкой рядом с «КС-2», чтобы видеть разрыв «выполнено по журналу / закрыто актами». Основание при правках закрытого месяца здесь не спрашивается — двигать нечего.

### «План месяца»
Таблица месяцев: остаток, число расчётных месяцев, план, факт, процент. Кнопка **«Зафиксировать»** замораживает план месяца — после этого новые ДС и акты его не меняют.

Сумму плана можно изменить кликом по значению, но только у **зафиксированного** месяца и только руководителю эк. отдела или админу. Требуется обоснование; оно вместе с автором и датой видно под суммой, а прежняя ревизия остаётся в истории.

### «История»
Вся цепочка действий по объекту: «Акт КС-2 подписан», «Договор изменён» и т. п., под заголовком — изменённые поля в виде «было → стало», ниже дата, автор и основание.

---

## 5. Сквозная проверка на демо-данных

Готовый сценарий заливается скриптом — вводить 20 актов руками не нужно:

```bash
cd fot-server
npx tsx scripts/seed-object-kpi-demo.ts                      # dry-run + список руководителей
npx tsx scripts/seed-object-kpi-demo.ts --manager=<id> --apply
```

Что он создаёт по объекту **«База Химки»**:

| Параметр | Значение |
|---|---|
| Договор | с 01.01.2025, срок 3 года |
| Плановая ЗОС | 31.12.2027 |
| Стоимость с НДС | 5 000 000 000 ₽ |
| Выполнено | 2 500 000 000 ₽ — 20 актов по 125 000 000 ₽, с января 2025 по август 2026 |
| Закрепление | руководитель с 01.01.2025, бессрочно |

Откройте «KPI объектов» и выберите в фильтре «База Химки». Таблица покажет все месяцы договора; в **строке августа 2026** (если это текущий месяц — она первая) должно быть:

| Показатель | Ожидаемое значение | Откуда берётся |
|---|---|---|
| Контрольная дата | **31.03.2028** | над таблицей: плановая ЗОС 31.12.2027 + 3 месяца |
| Договор с ДС | 5 000 000 000 ₽ | стоимость договора (ДС нет) |
| КС-2 | 2 500 000 000 ₽ | накопительно, 20 актов × 125 млн |
| Остаток | **2 625 000 000 ₽** | 5 000 млн − 2 375 млн (19 актов до августа) |
| Мес. | **20** | месяцев с августа 2026 по март 2028 включительно |
| План месяца | **131 250 000 ₽** | остаток ÷ 20 |
| Факт месяца | 125 000 000 ₽ | акт августа |
| % | **95,24** | факт ÷ план |

Затем зайдите под руководителем: «Личный кабинет» → «Мои объекты» → те же цифры по «База Химки», плюс сводка за период.

**Удалить демо-данные после теста:**

```bash
npx tsx scripts/seed-object-kpi-demo.ts --remove --apply
```

Скрипт удаляет только то, что создал сам (метка `KPI-DEMO`), и откажется работать, если по объекту появился настоящий договор.

---

## 6. Дополнительные проверки

- **Фиксация плана.** На вкладке «План месяца» нажмите «Зафиксировать» на августе. Затем добавьте и подпишите ДС на +50 млн с датой вступления в силу 01.09.2026 — план августа не изменится, план сентября вырастет. В строке августа появится значок **!** — «после фиксации изменились исходные данные».
- **Ручная правка плана.** Пересмотр зафиксированного месяца доступен только руководителю эк. отдела и администратору, требует основания и создаёт новую ревизию — старая остаётся в истории. Плановое значение, заданное вручную, помечается значком **✎**.
- **Автофиксация.** По умолчанию выключена; срок — 5-й рабочий день месяца по производственному календарю (`system_settings.object_kpi_fix_working_day`). На экране дата больше не показывается.
- **КС-6 не влияет на расчёт.** Запишите и подпишите КС-6 на любую сумму: меняется только колонка «КС-6», а план, остаток, факт и процент остаются прежними.
- **Формулы премии на настоящем PostgreSQL.** Денежная математика вынесена в два запроса, которые не читают таблиц, поэтому её можно прогнать на любой базе, включая read-only:

  ```bash
  cd fot-server
  OBJECT_KPI_SQL_TEST_URL=<строка подключения> \
  OBJECT_KPI_SQL_TEST_CA=<путь к yandex-ca.pem> \
  npx vitest run src/services/object-kpi-premium.sql.test.ts
  ```

  Без переменной набор пропускается — обычный `npm run test` от доступности БД не зависит.
- **Шкалу нельзя переписать задним числом.** `UPDATE`/`DELETE` версии, месяц действия которой уже наступил, отклоняется триггером: новый приказ — новая версия с будущей датой.
- **Удаление объекта.** Объект, по которому есть договор или акты, удалить нельзя — вернётся ошибка с перечислением того, что мешает. Такой объект архивируется, его прошлые месяцы остаются в отчёте.
- **Накопительный итог на полном периоде.** У объекта с заполненным «Первым расчётным месяцем» сверьте «КС-2» самого свежего месяца с суммой всех подписанных записей КС-2 по объекту (сумма **со знаком**, уменьшения вычитаются) — значения обязаны совпасть.

---

## 7. Что выглядит как ошибка, но ею не является

| Наблюдение | Объяснение |
|---|---|
| «—» вместо «0 %» | План месяца равен нулю или не определён. Полностью закрытый объект не должен выглядеть провалившим KPI |
| Таблицы нет, пока не выбран объект | Так и задумано: по умолчанию видны только виджеты за весь период |
| «По объекту нет договора» вместо таблицы | Объект есть в справочнике СКУД, но договор по нему не заведён — нажмите «Создать договор» |
| Период начинается позже даты договора | Начало берётся из «Первого расчётного месяца», если он задан: он и есть точка старта расчёта |
| Период начинается не с первого месяца договора | Окно ограничено 10 годами — на очень старых договорах ранние месяцы в отчёт не попадают |
| «Создать договор» на объекте с договором | Договор на объект один; сообщение «У объекта уже есть договор — правьте существующий» ожидаемо, существующий открывается кликом по строке месяца |
| Численность появляется с задержкой | Колонок численности на вкладке больше нет — показатель считается отдельным тяжёлым запросом и в отчёт не входит |
| Подписанный акт нельзя отредактировать | Подписанное только аннулируется, после чего заводится новая запись. Так история остаётся проверяемой |
| Просит основание при сохранении | У объекта есть зафиксированный месяц — правка исходных данных требует причины, она попадёт в историю |
| Уменьшение объёма отображается со знаком минус | Так и задумано: уменьшения вычитаются и из факта месяца, и из накопительного объёма |

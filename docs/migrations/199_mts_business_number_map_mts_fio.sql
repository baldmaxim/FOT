-- Миграция 199: ФИО владельца номера из данных МТС в привязке номеров.
--
-- XML-детализация/счёт МТС содержит пары «номер → ФИО» (<tp sim="номер"
-- u="ФИО">). При загрузке файла пары сохраняются сюда и используются для
-- автопривязки к сотрудникам ФОТ (точное совпадение нормализованного ФИО);
-- неоднозначные показываются в UI как подсказка «ФИО у МТС».
-- ФИО хранится открыто — как и ФИО сотрудников в проекте (employees.full_name).

BEGIN;

ALTER TABLE mts_business_number_map
  ADD COLUMN IF NOT EXISTS mts_fio TEXT;

COMMIT;

-- 151: убрать булев флаг «текущая деятельность» у отдела.
-- Режим «текущая деятельность» (одна строка на сотрудника без разбивки по объектам
-- в единой 1С-выгрузке) теперь определяется назначением объекта с адресом
-- (skud_objects.alt_name) = «Текущая деятельность» через department_object_assignment
-- (и переопределяется персонально через employee_object_assignment).
-- Заменяет миграцию 142. Применяется вручную через psql на проде (авто-миграций нет).

ALTER TABLE public.org_departments
  DROP COLUMN IF EXISTS is_current_activity;

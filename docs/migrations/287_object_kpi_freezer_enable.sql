-- 287: автофиксация плана KPI объектов на 3-й рабочий день (п. 2.8 приказа о KPI закрытия КС-2).
-- Применять ПОСЛЕДНИМ, уже после деплоя бэкенда и фронта:
--   1) экономисты исправили данные (ЖК Инжой: суммы ДС — изменением цены, а не полной ценой);
--   2) прошлые месяцы объектов проверены и зафиксированы кнопкой «Зафиксировать».
-- Включённая джоба в течение часа фиксирует ТЕКУЩИЙ месяц у всех объектов без снимка —
-- по тем данным, что лежат в базе в этот момент.
--
-- Фиксация закрепляет только плановую ЗОС, контрольную дату и число месяцев (п. 6.3).
-- Остаток и план закрытого месяца по-прежнему считаются по текущим данным, а правка
-- исходных данных такого месяца требует основания и попадает в историю.
-- Идемпотентно.

INSERT INTO system_settings (key, value, description, is_secret)
VALUES
  ('object_kpi_freezer_enabled', 'true',
   'KPI объектов: автоматическая фиксация плана месяца (kill switch).', false),
  ('object_kpi_fix_working_day', '3',
   'KPI объектов: рабочий день месяца, с которого план фиксируется автоматически (п. 2.8 приказа).', false)
ON CONFLICT (key) DO UPDATE
  SET value = EXCLUDED.value;
